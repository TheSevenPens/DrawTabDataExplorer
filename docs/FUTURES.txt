# FUTURES

Tracked work lives in GitHub issues. This file is just a thin index;
open the issue for current scope, design notes, and status.

## Open

- URL-encoded pipeline state for shareable views — #113
- In-app data editing (dev mode) — #114
- User-contributed data edits and suggestions — #115
- Text-based KQL input — #54
- Type-narrow .filter()/.sort() field parameter — #118
- Publish queriton to npm (Option C of #139 Phase 4) — #143
- SvelteKit major upgrade (clears 3 low-severity advisories) — #152
- Promote detail-page tabs to nested route segments (currently URL hash) — #211

## Shipped

- prefer-svelte-reactivity: audited as false positives (reassignment idiom), rule off + convention documented — #151
- Investigate replacing custom Query API with Arquero (decided: keep queriton; see #213) — #119
- Remove spring-decay extrapolation; backfill 0%/100% endpoints across 144 sessions — #212
- Write a user-facing tutorial for queriton — #146
- Add Palmer Penguins fixture to queriton test suite — #144
- Split FieldDef into core + UI display extension — #142
- Extract shared pressure chart session state — #172
- Centralize SubNav tab definitions — #173
- Add buildTabletNameMap and fix canonical naming bypasses — #174
- Migrate BrandDetail to shared Tabs component — #175
- Extract CompatEntityTable for pen/tablet cross-ref tables — #178
- Extend Playwright smoke tests (analysis, data-quality, session detail) — #179
- Entity redirect helper + EntityListLayout for route boilerplate — #180
- Add AGENTS.md entry point for AI agents — #181
- Deduplicate CLAUDE.md vs ARCHITECTURE.md roles — #182
- Add WHERE.md goal-to-files lookup — #183
- Add RECIPES.md task guides — #184
- Add verify-docs staleness script — #185
- Cross-link Explorer and data-repo docs — #186
- Clarify FUTURES / FUTURE / WORKSTREAMS roles for agents — #187
- Add ANTI-PATTERNS.md checklist — #188
- Add Agent note headers on hot files — #189
- Add Cursor rules pointer-linking to CLAUDE.md — #190
- Add DOC-STYLE.md for LLM-friendly docs — #191
- Add TESTING.md for agent verification — #192
- Generic Query/Pipeline API extracted to queriton workspace package — #139
- Split TabletDetail into tab-scoped subcomponents — #171
- Modularize data-quality page into section components — #176
- Modularize tablet-analysis page into section modules — #177
- Extract <Tabs> component for detail pages — #163
- Extract <SectionedPage> for sidebar-tree pages — #164
- Extract <DistributionTable> for bar-fill count tables — #165
- Merge pressure-bands.ts and spec-bands.ts into one band module — #166
- Extract shared <PmaxTab> (PenDetail + PenFamilyDetail dedup) — #167
- Factor inventory cellLinks boilerplate into makeInventoryCellLinks() — #168
- Unify withinYears() and filterByYears() in tablet-analysis — #169
- Compact numericSections via per-metric factory functions — #170
- Mechanical lint cleanup: require-each-key + no-navigation-without-resolve — #150
- Promote byYearDesc to shared helper in tablet-helpers.ts — #153
- Apply Type+Year+Export upgrade to TabletDetail / PenFamilyDetail / BrandDetail — #154
- Replace TabletDetail 'Copy as HTML' button with ExportDialog — #155
- Pin pptxgenjs to its own Vite chunk via manualChunks — #156
- Migrate TabletDetail Similar/Specs copy buttons to ExportDialog — #157
- Extract <ExportTableButton> component to dedupe 6 call sites — #158
- Dedupe yearNum helper into src/lib/year.ts — #159
- Unit tests for compareTabletByYearDesc / comparePenByYearDesc — #160
- Enable project-aware ESLint to drop ~8 inline disable comments — #161
- Remove 4 unused-CSS warnings in PenDetail.svelte — #162
- Remove vestigial defaultSortField prop — #149
- Accessibility cleanup pass on list-page components — #148
- Plan-rewrite pass (queriton: filter consolidation, top-K, take/skip arithmetic, trivial-step drops) — #140
- queriton keyBy/collectBy field-resolution fix — #138
- Standalone vitest config for queriton package — #145
- Polish queriton package.json + README example smoke test — #147
- README for the queriton package — #141
- Case-sensitive filter operators — #137
- Query.concat() / .union() — #136
- Query.last() / .reverse() — #135
- 'between' filter operator — #134
- Query.unroll() — #133
- Query.keyBy() / .collectBy() — #132
- Query.antijoin() / .leftjoin() — #131
- .filterIn() / .filterNotIn() + 'in' / 'notin' operators — #130
- Multi-key sort overload — #129
- Query.skip() — #128
- Query.join() / .semijoin() — #127
- Query.derive({col: fn}) for query-time computed columns — #126
- OR / nested boolean filters in the pipeline — #125
- Predicate-function overload on Query.filter() — #124
- Query.distinct() / .values() shorthand — #120
- More summarize aggregators (median/first/last/distinctCount/collect) — #121
- Query.select() column projection — #122
- Document HAVING pattern in API Explorer — #123
- Aggregation in the pipeline (summarize step) — #112
- Export results as CSV / JSON — #3
- Static site deployment to GitHub Pages
