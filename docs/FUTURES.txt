# FUTURES

Tracked work lives in GitHub issues. This file is just a thin index;
open the issue for current scope, design notes, and status.

## Open

- URL-encoded pipeline state for shareable views — #113
- In-app data editing (dev mode) — #114
- User-contributed data edits and suggestions — #115
- Text-based KQL input — #54
- Type-narrow .filter()/.sort() field parameter — #118
- Investigate replacing custom Query API with Arquero — #119
- Split FieldDef into core + UI display extension — #142
- Publish queriton to npm (Option C of #139 Phase 4) — #143
- Add Palmer Penguins fixture to queriton test suite — #144
- User-facing tutorial doc for queriton — #146

## Shipped

- Remove vestigial defaultSortField prop — #149
- Accessibility cleanup pass on list-page components — #148
- Plan-rewrite pass (queriton: filter consolidation, top-K, take/skip arithmetic, trivial-step drops) — #140
- queriton keyBy/collectBy field-resolution fix — #138
- Standalone vitest config for queriton package — #145
- Polish queriton package.json + README example smoke test — #147
- README for the queriton package — #141
- Generic Query/Pipeline API extracted to queriton workspace package — #139
- Case-sensitive filter operators — #137
- Query.concat() / .union() — #136
- Query.last() / .reverse() — #135
- 'between' filter operator — #134
- Query.unroll() — #133
- Query.keyBy() / .collectBy() — #132
- Query.antijoin() / .leftjoin() — #131
- .filterIn() / .filterNotIn() + 'in' / 'notin' operators — #130
- Multi-key sort overload — #129
- Query.skip() — #128
- Query.join() / .semijoin() — #127
- Query.derive({col: fn}) for query-time computed columns — #126
- OR / nested boolean filters in the pipeline — #125
- Predicate-function overload on Query.filter() — #124
- Query.distinct() / .values() shorthand — #120
- More summarize aggregators (median/first/last/distinctCount/collect) — #121
- Query.select() column projection — #122
- Document HAVING pattern in API Explorer — #123
- Aggregation in the pipeline (summarize step) — #112
- Export results as CSV / JSON — #3
- Static site deployment to GitHub Pages
